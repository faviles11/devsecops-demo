# ZAP baseline rules configuration
# Format: <rule-id>	<level>	(<description>)
# Levels: IGNORE | INFO | WARN | FAIL
#
# Elevate to FAIL so the pipeline gates on these findings.
# Rule 10037: Server leaks version info via X-Powered-By header (info disclosure)
10037	FAIL	(Server Leaks Information via "X-Powered-By" HTTP Response Header Field(s))
